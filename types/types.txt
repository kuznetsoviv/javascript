Для примитивов создается объект обертка:
	let str = "Привет";
	alert(str.toUpperCase());

	let n = 1.23456;
	alert(n.toFixed(2));

Такой код не работает:
	let str = "Привет";
	str.test = 5;
	alert(str.test);

Числа:		